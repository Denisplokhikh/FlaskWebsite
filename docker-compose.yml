version: "3.9"

services:
  auth-service:
    build: ./auth_service
    ports:
      - "5000:5000"
    volumes:
      - ./auth_service:/app

  post-service:
    build: ./publication_service
    ports:
      - "5002:5002"
    volumes:
      - ./publication_service:/app